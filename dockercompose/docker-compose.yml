version: '3.1'

services: 
    db: 
        image: mariadb/server:latest
        container_name: mariadbtest
        restart: always
        networks: 
            - projectNetwork
            
    web: 
        image: apache:latest
        build: ./webapp
        depends_on: 
            - db
        container_name: webServer
        ports: 
            - "8080:80"
        restart: always
        networks: 
            - projectNetwork
            
networks: 
    projectNetwork: 
